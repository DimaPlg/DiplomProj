package com.example.fitnesappmember.global

object SqlTable {
    const val admin = "CRETE TABLE  ADMIN(ID INTEGER PRIMARY KEY AUTOINCREMENT,USER_NAME TEXT DEFAULT ''," +
          "PASSWORD TEXT DEFAULT '',MOBILE TEXT DEFAULT '')"
}